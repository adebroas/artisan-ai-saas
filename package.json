{
  "name": "artisan-ai-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'use service-specific build commands'",
    "dev": "concurrently \"cd apps/api && pnpm run dev\" \"cd apps/web && pnpm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}